package dev.syncforge.network

/** Default REST paths for [SyncHttpClient] (see `docs/REST_API.md`). */
object SyncHttpRoutes {
    const val PUSH_PATH: String = "/sync/push"
    const val PULL_PATH: String = "/sync/pull"
}