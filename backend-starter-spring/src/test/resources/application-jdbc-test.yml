spring:
  datasource:
    url: jdbc:h2:mem:syncforge-spring;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: true

syncforge:
  store:
    type: jdbc